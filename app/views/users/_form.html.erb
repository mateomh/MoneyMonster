<div class="main-space">
<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: "text-label" %><br>
    <%= form.text_field :name, class: "text-input" %>
  </div>
  <br>
  <div class="field">
    <%= form.label :avatar_url, class: "text-label" %><br>
    <%= form.text_field :avatar_url, class: "text-input" %>
  </div>
  <br>
  <br>
  <div class="actions">
    <%= form.submit 'Register', :class => "pseudo-button orange" %>
  </div>
<% end %>
</div>
